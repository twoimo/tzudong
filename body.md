## 개요
- 지도 렌더링 안정성 강화 및 데이터 페칭 캐시 최적화

## 변경 내용
- **네이버 지도 렌더링 최적화**: `NaverMapView.tsx`에서 불필요한 마커 재렌더링을 방지하기 위한 가드 로직(`map-render-guard.ts`) 도입 및 마커 관리 로직 개선.
- **음식점 쿼리 키 안정화**: `use-restaurants.tsx`에서 경계값(Bounds) 및 필터 조건을 정규화(Normalize)하여 불필요한 API 호출을 줄이고 캐시 적중률 향상.
- **테스트 코드 추가**: 레스토랑 병합 로직 및 지도 렌더 가드에 대한 단위 테스트(`tests-unit`) 추가.

## 테스트
- 모바일/데스크톱 브라우저 환경에서 지도 드래그 및 필터 변경 시 반응 속도 개선 확인
- `npm run test:unit` 실행 시 신규 테스트 케이스 통과 확인

## 관련 이슈
- (없음)
