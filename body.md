## 개요
- InsightChat 제어 영역 UI 간소화 및 사용성 개선

## 변경 내용
- **`apps/web/components/insight/InsightChatSection.tsx`**:
  - **기억 프로필 메모 입력창 제거**: 채팅 제어 바에서 사용 빈도가 낮은 '기억 프로필 메모' 입력란을 제거하여 시각적 복잡도를 해소했습니다.
  - **드롭다운 시인성 개선**: 
    - 모델 선택, 이미지 모델, 응답 모드 등 각종 설정 드롭다운의 `z-index`를 `z-[120]`으로 상향하여 다른 UI 요소에 가려지지 않도록 수정했습니다.
    - 드롭다운이 열렸을 때 부모 컨테이너의 `overflow` 속성을 동적으로 조절(`overflow-visible`)하여 메뉴가 잘리지 않도록 개선했습니다.
  - **코드 정리**: 입력창 제거에 따라 더 이상 사용되지 않는 `setActiveConversationMemoryProfileNote` 콜백 및 관련 변수들을 정리했습니다.

## 테스트
- 관리자 인사이트 챗 페이지 하단 제어 바의 구성이 간소화되었는지 확인했습니다.
- 각 설정용 드롭다운 메뉴가 잘림 현상 없이 정상적으로 표시되는지 검증했습니다.

## 관련 이슈
- (없음)
