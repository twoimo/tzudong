## 개요
- 모바일 상단 제어부 및 카테고리 바 UI 시각적 요소 조정 (가독성 및 터치 편의성 개선)

## 변경 내용
- `apps/web/components/home/MobileControlOverlay.tsx`:
  - **헤더 버튼 크기 조정**: 북마크, 알림, 사용자 메뉴 버튼의 크기를 상향 조정(`h-8 w-8` → `h-9 w-9`)하고 내부 아이콘 크기를 최적화(`h-4 w-4` → `h-[18px] w-[18px]`)했습니다.
  - **검색바 시각적 강화**: 검색바 전체 높이를 상향(`h-11` → `h-12`)하고 내부 검색 버튼 및 로고 크기, 글자 크기(`text-sm` → `text-[15px]`)를 조정하여 시인성을 높였습니다.
  - **카테고리 바 및 필터 버튼 조정**: 카테고리 버튼의 높이(`h-8` → `h-9`)와 텍스트 크기(`text-xs` → `text-[13px]`)를 키우고, 여백을 세밀하게 조정하여 터치 오동작을 줄이고 사용자 경험을 개선했습니다.

## 테스트
- 모바일 실기기 및 에뮬레이터 환경에서 버튼 및 검색바의 시각적 밸런스 확인
- 카테고리 바 스크롤 및 버튼 클릭 시 터치 반응성 확인

## 관련 이슈
- (없음)
