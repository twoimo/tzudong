## 개요
- InsightChat 제어 영역 드롭다운 모바일 반응성 강화

## 변경 내용
- **`apps/web/components/insight/InsightChatSection.tsx`**:
  - **드롭다운 자동 앵커링 구현**: `getDropdownHorizontalAnchorClass` 함수를 추가하여 뷰포트 너비와 메뉴의 위치를 계산하고, 메뉴가 화면 밖으로 나가지 않도록 `left-0` 또는 `right-0` 클래스를 동적으로 적용합니다.
  - **반응형 너비 제한**: 드롭다운 메뉴에 `max-w-[calc(100vw-1rem)]`을 적용하여 좁은 화면에서도 안전하게 렌더링되도록 개선했습니다.

## 테스트
- 모바일 환경에서 각 드롭다운(모델, 응답 모드 등)을 열었을 때 메뉴가 화면 우측 끝에 잘리지 않고 정상적으로 표시되는지 확인했습니다.
- 데스크탑 환경에서 기존의 왼쪽 정렬 방식이 유지되는지 검증했습니다.

## 관련 이슈
- (없음)
