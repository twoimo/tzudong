# Production release and write continuation

The owner requests completion of the remaining 33 items through verified production deployment and writes. The objective remains active and is not replaced by read-only readiness or passing tests. Prior protected main is `047ae53da5d45383a96cfd7ad1c2d69fb84a11c8`; existing producer freeze and production deployment remain in place until actual prerequisites are established.

The first continuation adds a repeatable read-only SQL/Python observation collector, covering the actual private privacy schema, policy tuple, six workflow-related retention configurations, private-table RLS flags, and aggregate catalog gaps. It provides metadata evidence without requiring the solo operator to hand-author assertions. It does not turn reference presence into approval or activate disabled retention classes.

Live discovery on 2026-09-05 confirmed a 50-entry hosted ledger ending in `20260804000500`, published Korean policy `2026-08-04.1`, and only one active retention class among 12. The tables live in `privacy_retention`; absence from `public` is not a missing-table incident. Existing identity-audit configuration must not be copied to other classes as an invented legal basis or period. A fresh official Advisor query still reports catalog findings; the approved consent-view owner-bridge exception must be assessed against its actual predicates/grants rather than blindly changed to security_invoker.

Next execution work remains: collect and bind the current hosted observation; reconcile it with the pending additive advisor migration and exact producer/capture/recovery requirements; obtain actual backup evidence; implement/operate canonical timed measurements; establish the remaining operator/privacy/provider evidence; then perform approved deployment and bounded writes with readback. No listed external item is marked complete by this source-only addition.

Local validation of this addition: 7 collector tests (including real private-schema PostgreSQL observations and minimized error/cleanup behavior) plus 15 source/reconciliation tests pass. Fixture intervals are test values, not production retention recommendations. Security CI runs the real PostgreSQL collector tests.

PR #2870 review identified that the initial Python receipt accepted caller-supplied project and revision labels without verifying their provenance. The follow-up replaces receipt(raw_metadata, labels) with receipt(connection, source_sha): it admits only the exact direct project endpoint over hostname-verified TLS, checks the same active connection before/after collection, verifies actual committed SQL and loaded executor bytes with Git replacement objects disabled, and executes the verified SQL snapshot. Local collect() remains unbound metadata. Version-1 Python receipts cannot independently establish target/source identity and must not be relabeled as corrected receipts. Separately retained MCP call evidence is not a Python version-2 receipt. Twelve collector/transport/source tests, including actual local PostgreSQL rejection and real temporary Git objects, cover the correction; mocked successful transport is not hosted evidence. PR #2870 remains blocked until the fix completes serialized promotion and its reviews are resolved. No external task is closed by this correction.

The next PR #2871 review found three further provenance gaps in the proposed v2 path: caller-controlled TLS trust/peer selection, timestamp-based cached executor code, and reuse of an already-open snapshot. The v3 correction therefore uses a source-file launcher in isolated Python mode, compiles the exact verified Git executor bytes, owns a fresh connection with the committed pinned Supabase CA and fixed endpoint, accepts credential fields only, and rejects non-idle connections before checking the new transaction's isolation/read-only state. V1/v2 Python receipts remain insufficient and are not relabeled. Fifteen targeted tests cover the correction, including real stale-snapshot rejection and a poisoned timestamp-matching bytecode cache; successful transport mocks remain test evidence only. No hosted v3 receipt or production write is claimed.

A further PR #2871 review demonstrated that inherited Git repository/object-store variables could redirect source lookup despite `-C`. The launcher now removes inherited Git overrides, disables global/system Git config, and explicitly anchors the Git and object directories discovered from the intended checkout. A real foreign-repository control reproduces the old redirection and verifies canonical source acceptance plus foreign commit rejection. Sixteen targeted tests pass; this remains local source provenance evidence, not proof of protected-branch admission or a hosted receipt.

The source launcher also rejects file-based `objects/info/alternates` and `http-alternates`, because empty environment alternates do not disable Git alternate-store files. A real foreign dangling commit regression proves the old lookup succeeds and the corrected launcher rejects both foreign and canonical receipts until the alternate configuration is removed. Seventeen targeted tests pass.
