# Kiro operational-readiness progress — 2026-09-05

The owner has authorized autonomous completion in this task. The current record is 1270/1310 checklist entries complete, with 40 open entries. Several open entries refer to the same external operation. They are not independent missing features. Production release and the G037 write-freeze exit have not been established.

## Source and preservation

The original dirty checkout and the earlier recovery checkout remain untouched. Their inherited tracked-diff digest is `1d8579fa57858e824d06b13016ec3a6be4e27ab4e8485133ebca127879c4a95b`. Recovery PR #2858 merged to protected develop at `f5ac706239c1aaf9c0c62ab1f6c539959c8cf70f` after its checks and 29 review-thread resolutions. Correction PR #2861 then merged to `cb8bcc376acf4ca3d4470f3be71eca7de717d750` after 28 effective checks, including actual Release and Promotion Path success. Strict protection and normal PR merging were independently read back. Runtime PR #2862 then merged to protected develop `0222bda7d83002bd3f9b9dd10bff4e87f402ae22` after 28 effective checks. Data/main promotion has not yet occurred.

The protected-develop Vercel preview for `0222bda7d83002bd3f9b9dd10bff4e87f402ae22` reached READY as deployment `dpl_CravJWdBXmS2gW3JxUcLQK5rsHsB`. Authenticated homepage HTTP readback returned the application. This independently confirms the earlier adapter packaging correction. Secondary routes and a full hydrated preview flow are not established by that receipt. No new production deployment or DNS mutation is claimed.

The fifth review correction addresses 16 additional develop-to-data findings: PR-scoped and lockfile-only dependency evidence, clean source readiness, threaded API dispatch, positive freeze clearance, Kubernetes Secret names, seed exclusion, nullable publication CAS, retained live Rust receipts and explicit removal approval, bounded verification and durable trigger halts, Python-compatible date/hash semantics, and persistent Collector offsets/export queues. See `review-runtime-corrections.v5.json` for source and verification bindings.

## Verified changes and current state

| Area | Observed result | Remaining work |
| --- | --- | --- |
| Recovery and runtime | Crawler/platform/Rust sources recovered; durable queue and agent results, protected promotion policy, real descriptor/Compose admission and Rust call sites corrected | Primary and correction PRs merged; follow-up CI/review and serialized promotion remain |
| Branch protection | develop, data and main require GitHub Actions Release and Promotion Path checks, fresh base, PRs and resolved conversations; administrators are enforced | Retain merge receipts and verify protected-base policy after bootstrap |
| Dedicated G037 credential | Restricted v3 role, 19 dedicated connection conditions, Keychain custody and dedicated GitHub-secret metadata were independently read back | Run the selected read-only controller mode from newly promoted protected main |
| Crawler and GDrive scheduler | Both workflows are temporarily disabled; repository write freeze is active | Re-enable after promoted main contains freeze-aware guards; observe an actual scheduled start |
| Mac scheduler | Installed 05:15 wrapper from a separate stable approved develop checkout outside Documents, with Python 3.13.15. LaunchAgent, wrapper hashes, active freeze and manual held result read back | Update to final approved main before freeze exit; observe an actual sleep/wake boundary |
| Optional S3 | Pinned Chainguard MinIO/server-client images passed 11 real Storage-adapter operations, including multipart, negative authorization and restart persistence; test containers were removed | Keep optional; no claim of full Storage HTTP/DB integration, all-architecture validation or zero vulnerabilities |
| Hosted observability | Two read-only Supabase samples over 96.1 seconds showed 60 commits and zero new rollback/deadlock/conflict; exact Vercel production deployment remained READY | Provider state alone does not prove a scheduled run or end-to-end request |
| Hosted policy | Database, public API and hydrated browser matched Korean policy 2026-08-04.1 and the published hash | Remaining retention/backup/provider/legal and hosted catalog prerequisites |
| Auth protection | Leaked-password protection remains disabled; current Free plan is ineligible and requires Pro or above | Activation is conditional on plan eligibility; no purchase or fabricated activation receipt |
| GDrive | Current retained status has 191,849 residual of 192,095 items, with 246 verified | Validate actual remaining frames and recover within approved source/hash boundaries |
| Rust default | All five native components built and actual Python call sites exercised; independent Python reference preserved | Live N=3 evidence before default admission or shim retirement |
| Compiler benchmarks | All four retained x64 npm/Bun lanes were independently validated; Windows Bun legitimately admitted zero improvements because of noise | Frozen-tree results do not transfer automatically to later commits |
| G003 application performance | No retained real application cohort satisfies the canonical window and sample contract | Real 24-hour measurements and 168-hour field CWV; scoring, validation and detached artifact-map SHA |
| Production | Existing READY deployment `dpl_8fR6mDqD3SeBY6MXxFJ4iTvgh9fv`, source `29e432f7d96d98e6d09ba9237f05d6812ceb956b`, retained as rollback target | Exact tzudong identity, current gates, protected source, release and post-alias HTTP receipt |

## Evidence limits

The primary review batches have dedicated `review-runtime-corrections.v1/v2/v3/v4.json` receipt bindings. The latest local verification includes 202 tests in the 22-module orchestration audit, 19 native call-site/parity checks and 61 Rust tests. These groups overlap other regression suites and are not an additive unique-test count. Each result is scoped to the source and environment recorded with it. The latest follow-up passed 54 scheduler/storage and boundary tests, 48 focused web tests, seven promotion-policy tests, 30 additional source-provenance and scheduler/evaluator tests, native/compat TypeScript parity, scoped ESLint and shell syntax. The earlier 71-test integration remains scoped historical evidence. The actual Vercel-adapter build and file-map readback use Next 16.2.12 and adapter 4.20.4 without production credentials.

Earlier broad evidence — 2,047 web unit passes, 1,118 backend tests, compiler parity, production build and disposable local-stack/browser verification — remains historical evidence for its recorded source. It does not replace current-head CI. The initial source inventory is retained externally by SHA and in Git history.

Performance reports must retain absolute, relative and noise budgets. Typecheck duration budgets remain median 250 ms/5% and p75 250 ms/10%; RSS p95 remains 128 MiB/15%, with 5% MAD noise limits. G003 follows its canonical 36-row budgets. No application-performance improvement is claimed.

The task transcript supplies owner authority; it cannot supply a provider result, a real elapsed measurement window or a legal review. The freeze stays active until its actual exit conditions are verified. Refer to `closure-inventory-20260905.json` for the precise open checklist entries and receipt bindings.

Five completed boundary/authority entries were reconciled with the current task decision and fail-closed source tests. This does not close any pending migration, freeze exit, provider result, measurement cohort or legal review.

PR #2862 review identified two remaining issues. The S3 overlay now uses Compose reset tags to remove both inherited filesystem mounts; real merged renders showed Storage/imgproxy mount counts change from 1/1 to 0/0 while preserving the separate MinIO volume. Promotion now removes the PR-defined bootstrap entirely and runs only from the exact protected default-branch workflow revision via `pull_request_target` and `github.workflow_sha`. The bounded installation plan temporarily selects reviewed, protected develop as default, keeps the eight write-producing scheduled jobs held by the active freeze, and restores main immediately after serialized workflow installation or if installation stops. The temporary metadata change was executed and independently read back with unchanged branch protections. The default currently remains develop during installation; restoring main is mandatory after canonical promotion or if installation stops. See `.github/PROMOTION_POLICY.md`. The later protected-develop preview reached READY as recorded above; prior ENOENT remains historical.

GitGuardian incident 36950976 contained three required-environment template expressions. Its single-incident false-positive classification was independently reloaded, and a supported UI rescan produced GitGuardian check 101218179066 with success on source `3017996472d29955240f8578e6c79c4e046f727a`. Scanner settings were unchanged. The prior failed check remains historical; no custom passing status was created.

The Collector crash test used an isolated internal Docker network and synthetic log markers. After SIGKILL, the persistent exporter queue delivered its pending record, the receiver read a record appended during downtime, and the previous checkpoint prevented replay of an already delivered record. All fixture containers, the private volume and network were removed. This proves the tested local restart path, not hosted log delivery.

PR #2863 additionally requires canonical performance admission for Rust defaults and Python removal. The retained map, raw/scored files, frozen Git commit/tree, backend sample minima, operator binding and actual independent validator are checked. A healthy, validated zero-admission result remains valid; unavailable observations cannot masquerade as that result. The 76-test gate/adapter suite passed. This adds no measured improvement claim. External receipt SHA-256 `f581fedc991688d926c0d332ead5c71d7c55744e8fe1474a39000625ad6fafa2`.

The next #2863 review pass packages root-owned performance and promotion evidence plus pinned Node 24 in every pipeline image target. Retained Git commit objects independently bind the tree without Git or `.git` in the image. Validation is cached once per immutable receipt/slice/artifact/commit binding; measurement-source attestations bind exact Rust execution captures and observations. Approval timestamps are parsed as real UTC instants. The 89-test gate/container suite and 23 provenance tests passed. An actual non-root, read-only worker image passed the real canonical zero-admission fixture, reused its verdict for 100 calls without another subprocess, and rejected another slice/artifact. These are synthetic verification fixtures, not live parity or performance measurements. Receipt SHA-256 `f4db46db618d84d5271d6ff56276434edcdb70628d228fcb900e5f26e2d5f765`.

The subsequent #2863 pass compares lockfile-only candidates with the GitHub-resolved PR merge base, explicitly rejects RFC3339 end-of-day rollover across Python versions, and makes agent trigger admission atomic with its reservation. The new local-only, security-invoker RPC uses a trigger-scoped transaction lock and denies stale-snapshot isolation; the in-memory adapter also serializes admission. Two PostgreSQL connections that both read clear produced exactly one created and one halted reservation; two racing agents invoked only one executor. The focused 67-test suite passed on Python 3.13.15 and 3.14.7, 31 dependency tests and scoped ESLint passed, and 23 source-provenance tests passed. The additive migration has not been applied hosted. Receipt SHA-256 `4067cb53a1eef5cd9fabd5a1e338e6b1d0c647198364b56416f879295aa25e9c`.

The latest #2863 pass rejects duplicate JSON keys at every depth, non-finite numbers and non-UTF-8 receipt encodings. Cached performance admission now checks every nested artifact, runtime capture, schema/budget pin and validator file before reuse; changed files must pass their original hashes and full validation again. Missing files, aliases, atomic replacement, and same-size edits with restored mtime invalidate a verdict. The POSIX cache trusts OS change metadata; other platforms do full uncached validation. The 86-test Rust gate suite, 20 receipt/cache tests on Python 3.14, and 23 provenance tests passed. Eleven evidence roles were independently tampered. A real non-root Docker process with a writable bind mount denied modified evidence after a successful first validation, while the immutable image still reused a verified result for 100 calls without spawning another validator. Receipt SHA-256 `b68ccda4d21b090652ff4d4a41e79cb1b3cd8ff917c85c8b2592835627d5ce34`.

Nine more review findings are corrected in the current candidate: worker scheduler/checker source packaging and Node runtime libraries, actual deployment environment names, IEEE-754 private IPC parity, complete type-generation markers, Collector minimization and first-start tailing, resumable approval waiting, and definition-aware schema comparison. The worker image passed actual source/environment preflights as UID 10001 on Linux arm64; only the subsequent live entrypoint was stubbed. The Collector retained pending and downtime records after SIGKILL, excluded historical first-start records and forbidden synthetic values, and kept a seeded legacy raw queue held during upgrade. Filelog exports only bounded component, severity, type, UTC timestamp and UUID correlation fields; richer structured records still use the independent Python redaction path. Six real PostgreSQL constraint mutations plus NOT NULL drift were detected without retaining definitions. Partial pg-meta output left the previous type file unchanged, and native/compat compiler parity passed with zero diagnostics across 2,322 inputs. Test groups overlap and do not establish hosted release or live measurement results. Receipt SHA-256 `430f734c8b61840e1a535f59dc9031d09bfd02bf99a059cfce7114da5b6be81c`.

The Ubuntu CI Collector upgrade fixture exposed a root-owned sink output file. The test now reads only the newly appended payload suffix and preserves all prior evidence, with no chmod or truncation. The two real local Collector tests passed after the correction; the exact-head remote rerun remains required. Receipt SHA-256 `a04b6be1e192ba7b1760829e94116b58969a27c67d950e2ddbd2ccf282181d5e`.

The next CI run passed the Collector crash/upgrade test but found its stale candidate-only provenance binding. That binding is refreshed; all 62 operational and 36 Rust file bindings match, and the 15 provenance/reconciliation tests passed. Product runtime bytes are unchanged. Receipt SHA-256 `6c3e110d141475cb4aec5dd57a95cb762f47422296bcc6af366aae60859f24a9`.
