# G037 hosted migration closure runbook

## Authority, scope, and hard stops

GitHub Actions is read-only for G037. It is dispatch-only from the exact detached SHA of protected `main`, uses the `g037-hosted-closure` concurrency group, and permits only source `validate` plus remote `preflight`, `readback`, `runtime-probe`, and `reconciliation-readback`. `validate` is the default and has no credentials. Never run production execution or any production apply in Actions; Actions has no private signing-key, authorization, recovery, or freeze-evidence path.

The controller binds the current commit to the authoritative G034 manifest: exactly 29 migrations, in its pinned order and hashes. G026 and versions `20260713002500`, `20260713002600`, and `20260713002700` are excluded and must never enter the selected ledger. `public.restaurants_backup` absence is a hard preflight stop. G035 receipts are recovery evidence only; they do not authorize production execution. G036 recovery closure must be bound before local execution.

Remote denial receipts expose only `ambiguous_commit` plus an optional `denial_code` from the controller's closed allowlist. The code may distinguish credential admission, connection, ledger, retirement, lock, terminal-mutator, and unexpected-execute-privilege phases, but it never carries provider text, endpoint, credential, SQL, role identity, or row data. An absent or generic code is not evidence for any more specific cause. Every retry or different diagnostic mode requires a new exact-revision human authorization.

Remote read-only modes accept only the repository secret `SUPABASE_G037_READONLY_DB_URL`. Never fall back to the owner-capable `SUPABASE_DB_URL`, which remains outside this workflow. The G037 role must be a dedicated login with no database ownership, superuser, BYPASSRLS, CREATEROLE, CREATEDB, replication, target-mutator EXECUTE, or inherited owner authority; it must default to read-only transactions and receive only the bounded catalog and migration-ledger read scope required by this controller. Provisioning is an external Preview → Confirm → Apply → Readback → Audit operation and must complete before a new exact-revision run is authorized.

The exact operator preview is hash-bound by `g037-readonly-role-provisioning-preview.v1.json`. `g037_readonly_role_provisioning.sql` is intentionally outside `migrations/`, creates the login with `PASSWORD NULL`, and never mutates the migration ledger. It may be executed only once after named-owner approval of the exact source digest. Run the separate `g037_readonly_role_readback.sql` as the owner and retain only its bounded fixed-key result plus source/receipt hashes. Set the generated password afterward through a non-echoing external password-manager path, assemble the encrypted direct DSN only in external custody, and write only the resulting value to `SUPABASE_G037_READONLY_DB_URL`. Never put the password or DSN in a shell argument, file, log, artifact, issue, receipt, repository, or chat. Neither operator SQL file is a workflow entrypoint.

Named owner 최연우 approved preview SHA-256 `59489e04a707651c11b403d0c72831fe63573b2c27ef212e306042bf3dad82d9`, its bound provisioning/readback SQL digests, and the external credential-custody procedure. This is review approval only. A separate exact one-time hosted-apply authorization is still required before the pre-apply readback or any production mutation; no role creation, password configuration, repository-secret mutation, or workflow dispatch is authorized or evidenced by the preview approval.

The separately authorized v1 execution count was consumed once. Its postcondition rejected a zero-dimensional empty ACL substitution before commit. Bounded rollback readback proved the role remains absent and the migration ledger remains at 50 rows with terminal version `20260804000500`; retain only fixed code `acl_array_dimension_postcondition_denied`. Never retry the v1 SQL or reuse its authorization. Corrected preview v2 SHA-256 `9351623d80b179e46b335e65a6fd67faf86485783fa7e5e1117022cef4261fb3` changes only the three nullable column-ACL expansions. Named owner 최연우 approved that exact preview and both corrected SQL digests. The separately authorized v2 execution was consumed once; its generic role postcondition denied commit, and immediate readback again proved role absence and the unchanged 50-row terminal ledger. Never retry v2 or reuse its authorization.

All declared v2 postconditions pass in a disposable PostgreSQL 17.11 reproduction, and bounded hosted catalog reads exclude PUBLIC target-function EXECUTE and database/public-schema CREATE. Do not guess the remaining hosted-only difference. Diagnostic preview SHA-256 `8280b2848fbdcd7209affcf8eeb4aa539afe460defdacb97fc5552b84db7693b` binds `g037_readonly_role_postcondition_diagnostic_v1.sql`, which transiently reproduces the role and grants, raises only the first of 17 fixed codes, and always aborts before persistent commit. Named owner 최연우 approved that exact preview and diagnostic SQL digest, then separately authorized one production execution. All six preconditions passed. The authorization was consumed once and the diagnostic returned `g037_diag_memberships_not_zero`; immediate readback proves the transient role remains absent and the migration ledger remains at 50 rows with terminal version `20260804000500`. Never retry diagnostic v1 or reuse its spent authorization. This fixed code does not reveal membership direction, count, adjacent role identity, or effective privilege.

Membership diagnostic preview v2 SHA-256 `cdb21a3f3cfe6fe52cb37efbc3c40226a5425688321378ff3dbe6a41cf920394` binds SQL SHA-256 `29b7ef6084eb2732e65028dd1a0158a8cf5eec2ac60b3c081ddffd9d8461ad8a`. It reproduces the same transient sequence and distinguishes bounded direction, cardinality, adjacent-role privilege category, and PostgreSQL 17 admin/set/inherit options through a closed 18-code allowlist without emitting role names. Disposable PostgreSQL 17.11 returns `g037_membership_diag_none` and leaves the role absent. Named owner 최연우 approved the exact preview and SQL digests, then separately authorized one production execution. All six preconditions passed; the authorization was consumed once and returned `g037_membership_diag_has_elevated_member`. Never retry membership diagnostic v2 or reuse its authorization. Code priority proves only no parent membership, exactly one member, and one member matching the bounded elevated predicate. It does not prove member identity, grantor, login, or admin/set/inherit options. Three identical readback queries were issued only because the connector safety wrapper did not parse on the first two responses; the diagnostic was not rerun. The final bounded readback proves role absence and the unchanged 50-row ledger terminating at `20260804000500`.

PostgreSQL 17 documents an automatic grant when a non-superuser `CREATEROLE` user creates a role: the created role is granted back to the creator by the bootstrap superuser with ADMIN true, SET false, and INHERIT false. Creator-membership preview v3 SHA-256 `25b85850bbaef428915d88606276785777213b70c8828d3dc6006ab59ae84a2f` binds `g037_readonly_role_creator_membership_diagnostic_v3.sql` SHA-256 `9df2ba5db3e7a527d49df00025f15504fb29003fb543e6c3a735dc5d92a43e4b`. The rollback-only SQL requires zero parent memberships and exactly one current-user member whose executor is non-superuser `CREATEROLE`, grantor is superuser, ADMIN is true, and SET/INHERIT are false. It emits only one of 11 fixed codes and no role names. A disposable PostgreSQL 17.11 database-owner non-superuser `CREATEROLE` run returns `g037_creator_diag_creator_admin_only` and leaves the role absent. Named owner 최연우 approved both exact hashes; approval artifact SHA-256 is `8cc2520b8c7e667de009b34312db742fcb0eb64f50dc4ca7210944a7fda71108`, then separately authorized one production execution. All six preconditions passed. The authorization was consumed once and production returned `g037_creator_diag_creator_admin_only`, proving the same observable creator-admin shape without recording member or grantor identity. One immediate bounded readback proves role absence and the unchanged 50-row ledger terminating at `20260804000500`. Never retry creator diagnostic v3 or reuse its authorization.

Corrected provisioning preview v3 SHA-256 `bf1913e5b0cfffaad856e4080939d21e7d11d60fee4f2c333c92c3c9ca219ff9` binds provisioning SQL SHA-256 `77542865f044f10c8d6d86f99ba015ef3419a3dfacf5e4ad9c44cc8848d6c4a8`, readback SQL SHA-256 `8a225381a61ff1c752bcdee89722d16e9173a1aadda0fbcc35707cb9cbdeae09`, and controller source SHA-256 `188095df7df30edbe890d3cb0df9b1d69f59b7942dd449ae5f0b7b8fad5e89b0`. Provisioning keeps the v2 role flags, grants, password-null state, transaction boundary, and migration-ledger exclusion, replacing only zero membership with eight exact predicates for the production-proven safe inbound creator-admin shape. Readback exposes bounded counts and booleans only. Every connected controller mode applies a 33-predicate admission with zero parent membership, the exact safe inbound shape, and strict NULL column-ACL expansion. Disposable PostgreSQL 17.11 commits v3 and returns exact readback with target EXECUTE denied and zero owned objects. Named owner 최연우 approved all four exact hashes; review-approval artifact SHA-256 is `1547c6532e786f4a9767e546da0f2b43c55c488cd4266272d6eb3877b4899581` and binds the consumed creator-diagnostic authorization and attempt. Obtain a distinct one-time production apply authorization next. Diagnostic authority does not authorize provisioning, and neither review nor diagnostic evidence authorizes role creation, password setup, repository secrets, controller dispatch, migrations, or release.

Apply-authorization request v3 SHA-256 `a983a4f6ca843a3dc8d5ea9991d6bb2c66de5670ddbca0c1d96ed13f17abd9ef` binds that review approval, all four reviewed sources, the failed v2 apply, and the consumed creator diagnostic. It requests at most one production provisioning execution and one immediate bounded readback, subject to exact project, PostgreSQL 17, database, role-absence, ledger, target-function, and hash preconditions. The request is not authorization: its authorization count is zero, no named-owner execution approval or authorization artifact exists, and no production SQL may run from it. Passwords, credentials, repository secrets, controller/workflow dispatch, migration-ledger mutation, release, and deployment remain outside its scope.

Named owner 최연우 subsequently confirmed that this immediately preceding hash-bound request is approved. Apply-authorization v3 SHA-256 `0bf534d0db55f220d25fdbf9a9e727c3b665b32f7d5ea6ca9989d70f3fe20adb` binds the request and exact provisioning/readback/controller sources. It permits exactly one production provisioning v3 execution and one immediate bounded readback only after every project, version, database, role-absence, ledger, target-function, and source-hash precondition passes. It does not authorize password or credential work, repository secrets, controller/workflow dispatch, migration-ledger mutation, release, or deployment. The execution count remains unconsumed and no production state change is claimed until the separately recorded attempt proves otherwise.

The authorization was consumed before mutation under operation ID `d983a200-adc4-4c15-8521-f63e278bcc6b`; consumption artifact SHA-256 is `f9400c39d77d404f89536e101e2d4eb521df88d7b054d2559adceae4cf6db3ef`. Precheck matched the exact active healthy project, PostgreSQL 17, database `postgres`, absent role, 50-row ledger ending at `20260804000500`, and target function. Provisioning v3 executed exactly once without retry and returned without error. Exact readback v3 then executed once and proved the login present with all restricted flags, five settings, three bounded grants, zero parent memberships, the single creator-admin inbound membership, target-function EXECUTE denied, and zero owned objects. Sanitized attempt artifact SHA-256 `01a0c3ff861ecce1d2693a461ae989fbc7e6b2fa7bef4a7a64a9ef5c8cbba261` records fixed code `g037_readonly_role_provisioned`. The role still has `PASSWORD NULL`; password assignment, credential generation, repository-secret mutation, dedicated-role connection, controller/workflow dispatch, migration, and release remain unauthorized and unperformed.

Credential-custody preview v1 SHA-256 `43f50049495a0324491fa8fafc9e359e68e3f6bb0430c1c01a42af28c59cd113` is review-only and binds mutation-free dedicated-connection readback SQL SHA-256 `c927d42c6729d5516fc89921cc4cc5d7b84e8af9173b17f7c7403b2977834f28`. The proposed ceremony has five ordered stages: generate a unique password in an externally approved password manager; use an owner-authorized external libpq service/password profile and interactive `psql` `\password tzudong_g037_readonly` prompts without DSN/password arguments, echo, shell tracing, or terminal recording; construct the encrypted direct DSN only in external custody and run the exact one-statement readback as the dedicated login; write only that value to `SUPABASE_G037_READONLY_DB_URL` through secret UI input or standard input; then read only the secret name, presence, and update timestamp. Never touch or fall back to `SUPABASE_DB_URL`. Any password-assignment ambiguity, connection failure, or false readback boolean blocks the secret write. Review approval does not authorize password generation, password assignment, a connection attempt, a repository-secret mutation/readback, controller/workflow dispatch, migration, release, or deployment. Password assignment plus connection readback and the later repository-secret write each require their own fresh named-owner execution authorization.

The preview was checked against the current Supabase changelog and connection guide and PostgreSQL 17 `psql`/libpq documentation. No current Supabase breaking change alters this credential ceremony. PostgreSQL documents that `\password` encrypts the new value before transmission and keeps cleartext out of command history and server logs, and that `PGSERVICEFILE` selects an external service file. Supabase documents that a direct database endpoint is IPv6 unless an IPv4 add-on is already active and recommends SSL. Before credential mutation, the controlled host must therefore prove direct-endpoint reachability without enabling or purchasing an add-on, the external service must use `sslmode=verify-full` with a server root certificate, and the server must report `password_encryption=scram-sha-256`. An unreachable direct endpoint blocks this ceremony; do not silently substitute a pooler.

A credential-free check from the current workspace host resolved the project's IPv6 direct endpoint but could not reach TCP 5432. Attempt artifact SHA-256 `17d265b605258140f697f67caef17f3a028a16b521267d037ae85ccf48b71047` records fixed code `g037_direct_endpoint_tcp_unreachable`, no credential, no authentication, no SQL, and no persistent change; it does not establish a provider outage or firewall cause. Do not retry automatically. Password generation, assignment, dedicated DSN construction, and repository-secret mutation remain blocked until a separately approved controlled host proves direct reachability. A pooler or IPv4 add-on requires a distinct scope review and must never be inferred from this failure.

Named owner 최연우 then stated that approvals arising from the current work are approved. Because the exact credential-custody preview and connection-readback hashes had already been presented, approval artifact SHA-256 `46eea3b0f168775771f11b7909b8174fc8b3450ceb0fe23869ed28c2b744e3a8` records review approval for those bytes only. It does not preapprove unseen future artifacts or execution requests and does not waive the failed direct-network gate. Password-assignment authorization request SHA-256 `7b3602efed38f21d68b2015c8788baa19dabebb7da9d5e30b4d845be7a8bab7f` is therefore non-authorizing and remains blocked. It requests at most one external password generation, one interactive `\password` assignment, and one exact dedicated-role connection readback, but all controlled-host, direct-reachability, external-custody, psql 17, SCRAM, `verify-full`, and terminal-safety preconditions must be proved before a fresh execution authorization can be created.

Fixed-target probe SHA-256 `4fed6b4228fbc18db800ddcf543caa74babb3ac8b722aa91da1e3a34112702db` accepts only `validate` and `probe`, takes no host, port, endpoint, credential, timeout, or output path, and emits one canonical fixed-field JSON line. Controlled-host evidence request SHA-256 `9055ed5533a1008300cba6bc666ddaeff6503d87ce6d81ae93f1c94fef8f103c` requires one run from a different owner-approved host with IPv6 egress. From the exact reviewed candidate, run `python3 backend/supabase/scripts/g037_direct_endpoint_network_preflight.py probe` and provide only its JSON line. Do not provide terminal diagnostics. Only status `ready`, code `g037_direct_endpoint_ready`, both network booleans true, and all credential/database/state-change booleans false can satisfy reachability; a blocked result does not authorize retry or scope change.

Session-pooler alternative preview v1 SHA-256 `cdf4bd8f9c05eb2fd789228cdfffa563cf8b5dbf7e68f940b1c9689db8d8214e` is review-only and does not alter that direct-only contract. Current Supabase documentation permits shared Supavisor session mode on port 5432 as an IPv4-only alternative; transaction mode on 6543 is prohibited here. A read-only production project observation confirmed the project ref, `ap-southeast-1` region, active state, and PostgreSQL 17, but did not return an exact session-pooler hostname or username. Obtain those fields from metadata-only Dashboard Connect readback; never derive or guess them from the region, and never return a password or DSN. The candidate path must retain `sslmode=verify-full`, the external root certificate, the same dedicated read-only role, and all current-user and privilege admissions. Owner or `postgres` credentials remain forbidden. Preview approval does not select the pooler or authorize metadata reads, network probes, contract edits, password work, repository Secret changes, workflow dispatch, migration, release, or deployment; each later hash-bound step remains separate.

Metadata-readback request SHA-256 `f8101542b4f10bc8acaeb1bd657f7d5c0c1add9fbf30ccd456523255db9bcc22` is also non-authorizing and remains blocked until that exact preview is approved. After separate authorization, it permits one metadata-only Dashboard Connect inspection for the exact production project and Session pooler method. Observe only project/region, exact host, port, database, username suffix, certificate availability, and password-placeholder presence. Do not copy to the clipboard, capture a screenshot, retain a raw DSN, inspect browser storage or network headers, authenticate to Postgres, execute SQL, or run a network probe. The sanitized readback may retain only hashes, fixed codes, booleans, and bounded counts. Exact host or username must not enter a receipt; the host may enter only a separately reviewed fixed-target probe source. Any mismatch, ambiguity, unavailable method, or unexpected credential display stops without retry or scope escalation.

Before any authorized inspection, use the source-only `validate` mode of metadata-receipt verifier SHA-256 `0b3dff6e278b48695d0672e637b582c7edb8970156af92c5fe64b8395eeadba2`; receipt contract SHA-256 is `746a6c80cffd188ece7c39eb540216470154f1f09286d3e887a3836b7427fd81`. A future external receipt must be canonical one-line JSON, no larger than 8 KiB, in an absolute owner-only regular non-symlink file. `verify` performs no network, database, or write action. It requires exact preview/request hashes, UUIDv4, UTC timestamp, nonzero hostname/username-shape digests, all ten fixed metadata booleans true, and all nine prohibited-action booleans false. Any drift or custody failure returns only `g037_session_pooler_metadata_receipt_denied`. Success returns only fixed status/action fields and the receipt SHA-256; it never emits the receipt, hostname, username, endpoint, provider text, or credential. No actual receipt, approval, or external inspection is claimed by the prepared verifier.

Preview-approval request SHA-256 `2fbe7bbb5bd8b461c12d7d7cf723e8be9c040bc8eb90a6c321fb3ac8422c5804` asks for exactly `최연우, G037 session-pooler alternative preview v1 cdf4bd8f9c05eb2fd789228cdfffa563cf8b5dbf7e68f940b1c9689db8d8214e 검토 승인`. It targets only those preview bytes. Do not accept an unqualified affirmation or apply the earlier standing statement to this later artifact. The prepared metadata request, receipt contract, and verifier are bound for identification but remain unapproved. Even exact preview review approval would not select the pooler, abandon direct transport, authorize Dashboard inspection, prove a receipt, or permit a network, password, Secret, controller, workflow, migration, release, or deployment action.

Approval-contract SHA-256 `856d361821b28ae319eff627a40a57f5e32e4ba268b5978a220a8e22b9620566` defines the only acceptable future record without creating it. Evidence must be the exact current-task user message `최연우, G037 session-pooler alternative preview v1 cdf4bd8f9c05eb2fd789228cdfffa563cf8b5dbf7e68f940b1c9689db8d8214e 검토 승인`, posted after the preview and request. Assistant text, a generated/local statement, inferred identity, or earlier standing intent is not evidence. A later approval artifact must bind both hashes, record the exact statement and transcript boundary, leave successors unapproved, and keep every execution count zero. Its only effect is review approval plus permission to present the metadata request; it grants no metadata-read or external-action authority.

Named owner 최연우 then supplied that exact statement in the current task. Approval artifact SHA-256 `50d9c3b69fe5ecd2024378c67141a7f41081434ecaec3a7ed1da0783b3ef9279` binds the preview, request, and approval-contract hashes and records the exact current-task evidence boundary without using the earlier standing statement or assistant/generated evidence. The approval is review-only: it permits presenting the metadata request next but does not select the pooler, abandon direct transport, authorize Dashboard inspection, claim a receipt, construct or run a network probe, amend the credential contract, or authorize password, database, repository Secret, controller, workflow, migration, release, or deployment work. All successor reviews and execution counts remain zero.

Named owner 최연우 subsequently authorized exactly one production metadata-only Dashboard Connect read bound to request SHA-256 `f8101542b4f10bc8acaeb1bd657f7d5c0c1add9fbf30ccd456523255db9bcc22`. Authorization artifact SHA-256 `240b24087110c8e88c7859f3370f3fba21aa1bf6af4032410fefc99a7226a54f` preserved the no-screenshot, no-clipboard, no-DSN, no-credential, no-storage, no-database, no-SQL, no-network-probe, and no-mutation boundary. The offline verifier first returned `g037_session_pooler_metadata_source_valid`. Opening the exact Dashboard URL then consumed the sole read; consumption artifact SHA-256 is `d203751ccd74cbb14297a56139f71de412b2ef5eff11876e32176f98bb6664e5`.

The browser accessibility-tree operation returned unapproved project-overview fields before an exact Session pooler field read and then activated a non-Session connection control. The attempt was stopped immediately without screenshot, clipboard, DSN/password read, browser-storage or header inspection, database authentication, SQL, network probe, or persistent provider mutation. Denied attempt artifact SHA-256 `6e5915d6d4f2e96f4aa07ade8e10277b6daab382370dec8c85649f797c1d036b` records only fixed code `g037_session_pooler_metadata_scope_boundary_violated`; it does not reproduce the out-of-scope UI text. No sanitized success receipt exists, exact pooler metadata remains absent, the pooler remains unselected, and the authorization has zero remaining reads. Never retry it. A fresh selector-bounded request and fresh named-owner authorization are required before any later browser attachment or inspection.

Control-map request SHA-256 `48366c5e157a186a6c19647a70da40d027c01a70e83ba0e3b6087ec5679fca7f` narrows the next proposed external action to one control-only discovery. It allows one exact production Dashboard open, two accessibility snapshots limited to interactive buttons and comboboxes inside the Connect dialog, and one click on the exact `Direct Connection string` entry control. It may only identify that exactly one `Session pooler` control exists; it must not click that control or read project, organization, region, hostname, port, database, username, password-placeholder, certificate, textbox, input, code, preformatted, definition-value, page, main, whole-dialog, or arbitrary text content. It retains only a control-shape digest, fixed code, booleans, and bounded counts—never the raw control tree, control names, or locator references.

Offline control-map verifier SHA-256 `f5b641d76d33ed8343751dabb634f90737c898b4a44f70f95d2f63c27f601764` and receipt-contract SHA-256 `c696287a0246849ef774aa256eb27a982d4303c06e0fd9ed73a3f3a004e5d1f1` are source-ready. The verifier requires one Dashboard open, two control snapshots, one click, exact dialog/direct-entry/session-control booleans, a nonzero control-shape digest, and every metadata/sensitive/external-action boolean false. It is offline, reads only an owner-only canonical receipt, and emits only fixed status fields plus a receipt digest. The request is not authorization: control discovery, later metadata read, pooler selection, network probe, password, Secret, controller/workflow, migration, release, and deployment remain blocked pending fresh exact approval.

Named owner 최연우 authorized that exact v1 control-only request once. Authorization SHA-256 `3cfe7a899473aea6c31c2a50d1e644db4147c1e03fb3bf10f6118c759ed0f2a6` was consumed before the browser operation in artifact SHA-256 `61979c28e596a55a0dcf4fc21b54c518b8ebe9996139892cf80819e25da21748`. A non-interactive shell-argument form first failed syntax before a browser open. The same bound operation then submitted an interactive async-IIFE program, but the REPL returned before its asynchronous completion and produced no sanitized observation. Because whether the single `openTab` call completed could not be proved, the Dashboard-open budget was conservatively treated as consumed and no further browser command was issued. Attempt SHA-256 `9bcf5eec76211f11113172b08a1133a67a9824d77874212d6a57c0254dce669c` records fixed code `g037_session_pooler_control_map_async_completion_ambiguous`. No control snapshot or click completion, raw tree, metadata value, credential, DSN, screenshot, clipboard, browser-storage/header inspection, database authentication, SQL, network probe, provider mutation, or success receipt is claimed. Never retry v1.

A browser-free local REPL preflight then proved that top-level `await` returns a fixed completion signal. Reviewed browser source SHA-256 `a07dd10e07c0cd0a1db050230206b6e5ebfd4402c172556d1a159b571e3044bc` is one physical line, passes Node syntax validation, uses top-level `await` rather than an async IIFE, accepts no dynamic code or input, and preserves the same two button/combobox snapshots, one exact Direct Connection entry click, Session pooler identification-only, and no-value boundary. It outputs only a canonical v2 sanitized observation or one fixed denial; no raw tree, dynamic control name, locator, exception, or provider text is emitted.

Control-map v2 request SHA-256 `e74e85936b4d44776ffacf878a65604c03e91ac03b91f4416090ed5efecd0a08` binds the spent v1 authorization and attempt plus that exact browser source. Offline receipt verifier v2 SHA-256 `7b6be03f1ac15ee93f5b10ddf33d070382e5eb9939f7bab30857f71ce8a631eb` and receipt-contract v2 SHA-256 `15150608fcb788f847b8053f6fe61522717759206b6c5e405093bf81635bea1c` bind the request, source, prior attempt, observation digest, control-shape digest, exact counts, and negative action fields. Authorization request SHA-256 `d4259300e49459be899c4c35a06818e45cee814b02af6b0008a5dd234e3da900` requires the exact named-owner statement containing all four reviewed hashes. None of these v2 artifacts authorizes execution; a fresh exact statement and authorization artifact are required first. Syntax, transport, or ambiguous completion consumes that future authorization, with no retry.

Named owner 최연우 later supplied that exact v2 statement. Authorization SHA-256 `62b222c88728c0b993359dd472e36aa7fcfa715bceaac3dd6e1675f76c09e069` was consumed before Aside execution in artifact SHA-256 `27cbf11111867f223acfd92fb72675c4f105dffebb64a1d115ac68fdb8c7659f`. The exact one-line browser source ran once and returned canonical denial observation SHA-256 `422180f6518a408b11cb1155438826db884b5513c1d8c35a6919051f37ef1114`: one Dashboard open completed, but no restricted control snapshot or click completed; no metadata value was read and no provider state changed. The Aside transport itself emitted an automatic tab-title envelope containing project or organization labels, so repository evidence retains only that bounded fact and never the raw title. Attempt SHA-256 `f624f78125c1c3938053569fa27e2d547aa3796f17d621f7707f8eb47580aad5` records fixed code `g037_session_pooler_control_map_v2_denied`. No success receipt was created or verified. Never retry v2 under the spent authorization, and do not construct or execute a metadata-value read from this result.

Local-only follow-up performed no additional Dashboard read and does not claim the exact v2 failure cause. Installed Aside guidance confirms selector-scoped interactive snapshots are supported. Reviewed one-line v3 browser source SHA-256 `6dbf2915400970b6de301a2f9aed5c0736d23bc7572d8a6ec7e8e4ced3a5d96e` adds a maximum 15-second wait for the same allowed control selector before the first snapshot, a maximum 10-second wait before the second snapshot, and fixed denial stage codes; it does not expand nodes, values, clicks, or outputs. Reviewed stdout filter SHA-256 `f826640d6004d9baec9870130180fc189e75aa83b982dd14d4b44be8e1082855` accepts at most 32 KiB, discards every non-observation transport line without retention, and emits exactly one canonical v3 observation or exits non-zero. Control-map v3 request SHA-256 `e0e33500d568d911412c0b0faf4fe2ecf732185655c128f25c6ae6d45c72e9b0`, offline verifier SHA-256 `7c9d5033ea0e581df8e133310e8f8f923aa5313f8c8e84753bad4c3f6ed5c942`, receipt contract SHA-256 `1336c656acd8abb1bdf4e51a157400160e87a28bde4bd159e40fe7e36e172daa`, and authorization-request SHA-256 `3469f4f434627c1e49a7b8e9e5765c50e4d82548c6c40a6c014d1de5a57759d8` are source-only. V3 execution remains blocked until the exact fresh named-owner statement is received and consumed.

Do not retry a self-committing migration with an ambiguous commit result. Stop, preserve sanitized receipt hashes, and investigate through provider support and human operators. Do not infer success from a timeout, disconnect, or partial receipt.

## Human-only prerequisites

Before any remote read-only preflight or local production execution, a human must maintain the human-only provider/ingress fence and quiescence: disable or block all application, worker, dashboard, API, direct-SQL, and provider ingress writes; verify no in-flight writers; and independently verify that the human-only G037 write freeze is already active (`G037_WRITE_FREEZE=active`). An absent or mismatched freeze state blocks execution. This local-only procedure must not set or change `G037_WRITE_FREEZE` or any GitHub repository or environment variable. Operators must keep the freeze active through G038. Do not make any post-G037 claim that live account deletion or retention has resumed.

A human must collect fresh provider evidence for PITR/database recovery, Auth metadata/state recovery, Storage metadata recovery, and Storage/blob recovery RPO. Keep authorization, recovery, RPO, freeze, service, recipient, and signing-key materials in owner-restricted offline evidence/key paths. Backup material, provider exports, credentials, raw URLs, row data, keys, and secrets never go to GitHub repositories, Actions artifacts, caches, logs, arguments, releases, or issues. This procedure has a zero-cost/no-paid-service boundary: use no paid service or new paid backup destination.

Perform and record an offline rollback rehearsal before production execution. Keep only sanitized G036-bound recovery evidence with the closure record.

## Protected sequence

1. On the controlled local host, independently verify the already-active human-only G037 write freeze and collect exactly five fresh owner-restricted residual-evidence artifacts: no owner write, no dashboard write, no provider write, no out-of-band write, and producer stop. An absent or mismatched freeze state blocks execution; do not set or change `G037_WRITE_FREEZE` or any GitHub repository or environment variable.
2. Run `g037_production_controller.py prepare` only through the isolated exact-source bootstrap. It publishes a fresh canonical **unsigned** assertion request outside the repository after rollback-only preflight. Each of the five evidence files must use the G037 residual-evidence schema and name its exact channel and freeze ID. Producer-stop must contain exactly the `account-deletion-worker.yml` / `dispatch` and `privacy-retention.yml` / `retain` identities, their literal `vars.G037_WRITE_FREEZE != 'active'` guard, `guard_active: true`, and `no_in_flight: true`; missing, added, renamed, or swapped producers block.
3. Review the exact request bytes offline, create a detached signature with the fixed authorization public-key counterpart, and retain request and signature as distinct restrictive regular files. Run `finalize` through the same bootstrap; it holds both files by descriptor, verifies the detached signature over the held canonical request bytes, and publishes the finalized assertion. Never supply any private signing-key path on the controller command line.
4. Verify under restrictive external custody the legacy G035 capture, restore, and inspection receipts and the legacy signed authorization. Confirm their signature, ordered chain, manifest, remediation vector, and receipt hashes before proceeding.
5. Build and sign the G037 remediation template offline as its detached-signature ceremony specifies. Keep all private keys outside the controller CLI and under restrictive external custody.
6. `validate`, `rehearse`, and `execute` require the same seven legacy/execution files: legacy G035 capture receipt, restore receipt, inspection receipt, legacy authorization, legacy authorization signature, G037 execution authorization template, and its detached signature.
7. Run local-only `g037_production_controller.py validate` through the isolated bootstrap with the finalized operator assertion and all seven files. Validate is database-free and rejects custody, expiry, source, freeze, and evidence mismatches.
8. Run local-only `g037_production_controller.py rehearse` with fresh outside-repository receipt destinations. It remains rollback-only rehearsal evidence, but it atomically consumes that execution authority in the canonical authorization-derived journal before opening the database. Independently verify the signed rehearsal artifacts, then obtain a fresh request, detached signature, finalized assertion, and execution authority before execute.
9. Independently verify the recovery archive and signed rehearsal artifacts. Stop on any mismatch.
10. Before execution, pre-provision the owner-restricted attempt-journal directory at `C:\ProgramData\TzudongRecovery\g037-attempts` on Windows or `/var/lib/tzudong-recovery/g037-attempts` on POSIX; the controller never creates it and never derives it from `HOME` or another caller-controlled environment variable. Run local-only `g037_production_controller.py execute`. The controller derives the only valid marker location from the authenticated authorization ID and authorization hash: the directory is fixed by the operating system and the filename is derived from the authorization, so callers cannot select or override either. The marker is exclusively created and directory-synced before secret admission or database connection, its canonical digest is bound into prepared remediation evidence plus outcome and reconcile receipts, and it is retained on commit, rollback, ambiguity, and every failure. A second process cannot reuse the authority with a different output path. After **any** attempt, including rollback, obtain fresh request, detached signature, finalized assertion, and execution authority. No GitHub Actions job may execute this mutation.
11. After execute, ALWAYS run local `g037_production_controller.py reconcile` as the required post-readback gate to verify encrypted archives and live observed terminal roots and publish reconciled final/outcome receipts. Remote `reconciliation-readback` is supplementary only; dispatch it with readback and runtime-probe on the same bound commit while retaining the provider/ingress fence and write freeze through G038.
