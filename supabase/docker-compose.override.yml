services:
  studio:
    environment:
      DASHBOARD_USERNAME: ${DASHBOARD_USERNAME}
      DASHBOARD_PASSWORD: ${DASHBOARD_PASSWORD}

  # PostgreSQL 직접 연결 포트 (마이그레이션 스크립트용)
  db:
    ports:
      - "127.0.0.1:54322:5432"
