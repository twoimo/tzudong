/**
 * 도장 라우트 로딩 경계.
 *
 * 전체 페이지 스켈레톤을 여기서 그리면, 클라이언트 페이지가 준비된 뒤
 * 같은 스켈레톤이 한 번 더 교체된다. 정적 화면은 페이지가 유지하고
 * 목록 스켈레톤은 `isStampDynamicLoading` 한 곳만 소유한다.
 */
export default function StampLoading() {
    return null;
}
