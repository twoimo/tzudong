# ── 의존성 / Dependency ──────────────────────────────────
node_modules/
__pycache__/
.venv/
venv/
backend/bun.lock

# ── 빌드 / 출력 / Build & Output ─────────────────────────
.next/
.next-stale-*/
.turbo/
dist/
.vercel/
apps/web/.cache/
apps/web/tmp/
apps/web/public/qa-history/
output/
# Local verification/release artifacts (not product source)
artifacts/
apps/artifacts/
apps/web/artifacts/
nightly-artifacts/
apps/.release-visual-protected-replay/

# ── 로그 / Logs ──────────────────────────────────────────
logs/
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*
backend/log/
daily_run.log
run_*.log
backend/storyboard-agent/log.md

# ── 런타임 데이터 / Runtime ──────────────────────────────
pids/
*.pid
*.seed
*.pid.lock

# ── 환경 변수 / Environment ──────────────────────────────
.env
.env.test
.env.local
*.env.local
backend/.env
backend/.env.gemini-file-api.local
backend/restaurant-crawling/data/gemini_cookies.local.json
backend/restaurant-crawling/data/gemini_accounts.local.json
backend/restaurant-crawling/data/gemini_cookies.json

# ── OS 메타데이터 / OS Meta ──────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
*.desktop.ini

# ── IDE / 에디터 / Editor ────────────────────────────────
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ── Supabase / DB / Auth ────────────────────────────────
supabase/.temp/
supabase/backup_db/
backend/supabase/backup_db/*.sql
supabase/.env*

# ── 프로젝트 데이터 (대용량 / 임시) ──────────────────────
backend/restaurant-evaluation/temp/health_check_prompt.txt
backend/restaurant-evaluation/temp/health_check_response.json

# 대용량/자동 생성 폴더
backend/restaurant-crawling/temp/

# 브라우저 프로필 데이터
backend/restaurant-crawling/data/browser_profile/
backend/restaurant-crawling/data/camoufox_profile/
backend/restaurant-crawling/data/chromium_profile/

# ── CI / 인증 / 임시 문서 ─────────────────────────────────
summary.md
body.md
pr_body*.md
gha-creds-*.json
apps/web/tests/temp-*.spec.ts

# ── AI 코딩 / 에이전트 도구 (Codex/OMX/Gemini/OMC) ─────────
.omc/
.omx/
.codex/
.agent/
.agents/
.gemini/
AGENTS.md
!/AGENTS.md
OMX_MODERNIZATION_WORKLOG.md
# Local AI/browser helper state and packaged agent drops
.antigravitycli/
.tmp-gjc-*/
.local-*
.tmp-g034-*/
.tmp-g035-*.json
.tmp-g036-*.json
.venv-storyboard-rag/
gajae-code-coding-agent-*.tgz
/x
telegram-connection-test.txt
g028-quality-gate.json

# Root has no package.json; keep accidental root npm lockfiles out of public branches.
package-lock.json

# ── 크롤링 데이터 무시 ────────────────────────────────────
backend/restaurant-crawling/data/*/frames/
backend/restaurant-crawling/data/*/video_cache/
backend/restaurant-crawling/data/*/temp_video/
backend/restaurant-crawling/data/*/thumbnails/
backend/restaurant-crawling/data/frames/
backend/restaurant-crawling/data/credentials.json
backend/restaurant-crawling/data/cookies.txt
backend/restaurant-crawling/data/*.local.json
backend/restaurant-crawling/data/gemini_cookies.json
backend/restaurant-crawling/data/gemini_accounts.local.json
backend/restaurant-crawling/data/camoufox_profile/
# Local crawl/evaluation dumps. Tracked snapshots stay in git; restore
# mutations instead of committing blobs. Untracked jsonl stays ignored.
backend/restaurant-crawling/data/**/*.jsonl
backend/restaurant-evaluation/data/**/*.jsonl

# Operator timestamp, not a product file.
backend/.sync_trigger

# Local generated backend review/export reports; may contain live operational data.
backend/restaurant-evaluation/reports/

.vercel
.gjc/
tmp/

# Isolated recovery worktrees and local operator artifacts. Never commit
# dumps, keys, or hosted receipts from this tree.
.recovery/
